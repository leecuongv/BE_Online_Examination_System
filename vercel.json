{
    "version": 2,
    "builds": [{
        "src": "./index.js",
        "use": "@vercel/node",
        "config": {
            "maxLambdaSize": "50mb",
            "runtime": "nodejs22.x"
        }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/"
    }]
}